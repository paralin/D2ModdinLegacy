Template.self.matchList.rendered = function() {
  // things to do when the template is finished rendering
};

Template.self.matchList.events({
  //".my-button click": function() {',
    // do something clever',
});